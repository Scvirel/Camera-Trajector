using UnityEngine;

namespace CameraTrajector.Client
{
    public sealed class Constants
    {
        public static Vector3 DefaultCameraPosition = new Vector3(0f, 1f, -10f);

        public static Vector3 TargetLocation = Vector3.zero;//TODO:In future probably will dynamic value
    }
}